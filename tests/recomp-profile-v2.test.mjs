import test from 'node:test';import assert from 'node:assert/strict';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const e=require('../recomp-profile-v2.js');
test('meal shares sum to one for 3-6 meals',()=>{for(const n of [3,4,5,6])for(const p of ['balanced','breakfast','lunch','dinner'])assert.ok(Math.abs(e.shares(n,p).reduce((a,b)=>a+b,0)-1)<1e-9)});
test('balanced meal shares match the production planner policy',()=>{assert.deepEqual(e.shares(3,'balanced'),[.25,.40,.35]);assert.deepEqual(e.shares(4,'balanced'),[.22,.34,.14,.30]);assert.deepEqual(e.shares(5,'balanced'),[.20,.10,.32,.12,.26]);assert.deepEqual(e.shares(6,'balanced'),[.18,.10,.29,.11,.24,.08])});
test('priority patterns remain bounded and actually prioritize the selected meal',()=>{for(const n of [3,4,5,6]){const base=e.shares(n,'balanced');for(const pattern of ['breakfast','lunch','dinner']){const s=e.shares(n,pattern),idx=e.mealIndex(n,pattern);assert.ok(s[idx]>base[idx],`${n} ${pattern}`);assert.ok(Math.max(...s)<=({3:.45,4:.40,5:.36,6:.34}[n])+1e-9)}}});
test('meal targets reconcile exactly',()=>{const t={kcal:2441,protein:167,carbs:287,fat:69},m=e.mealTargets(t,6,'dinner'),sum=m.reduce((a,x)=>({kcal:a.kcal+x.kcal,protein:a.protein+x.protein,carbs:a.carbs+x.carbs,fat:a.fat+x.fat}),{kcal:0,protein:0,carbs:0,fat:0});assert.deepEqual(sum,t)});
test('macro energy reconciles after nutrition calculation',()=>{const x=e.nutrition({sex:'f',age:38,height:166,weight:68,activity:1.45,goal:'recomp'}).targets;assert.ok(Math.abs(x.protein*4+x.carbs*4+x.fat*9-x.kcal)<=4)});
test('training respects days',()=>{const lib=['Pecho','Espalda','Cuádriceps','Isquios','Glúteos','Hombros','Bíceps','Tríceps','Core','Gemelos'].map((muscle,i)=>({name:'E'+i,muscle,equipment:'Máquina',alt:'A'}));const p=e.training({days:5,minutes:50,equipment:['Máquina']},lib);assert.equal(Object.keys(p.routine).length,5)});