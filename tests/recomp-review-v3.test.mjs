import test from 'node:test';import assert from 'node:assert/strict';import {createRequire} from 'node:module';const require=createRequire(import.meta.url);const e=require('../recomp-review-v3.js');
test('recognizes positive recomp with stable weight and falling waist',()=>{const r=e.review({weights:[80,80.1,80,80,79.9,80,80],waist:[90,89.9,89.8,89.6,89.5,89.4,89.3],dietAdherence:.95,trainingAdherence:.9,performance:.2,weeksObserved:3,photoStandardized:true});assert.equal(r.status,'positive-recomp');assert.equal(r.calorieDelta,0);assert.equal(r.confidence,'high');});
test('does not adjust plan when adherence is poor',()=>{const r=e.review({weights:[80,80,80,80,80,80],waist:[90,90,90,90,90,90],dietAdherence:.5,trainingAdherence:.9,weeksObserved:4});assert.equal(r.reliable,false);assert.equal(r.calorieDelta,0);});
test('raises calories when loss is too aggressive and performance falls',()=>{const r=e.review({weights:[80,79.6,79.3,78.9,78.6,78.3,78],waist:[90,89.8,89.6,89.4],dietAdherence:.95,trainingAdherence:.9,performance:-.6,weeksObserved:2});assert.equal(r.status,'too-aggressive');assert.equal(r.calorieDelta,100);});
test('applied targets reconcile macro energy closely',()=>{const t=e.apply({kcal:2300,protein:170,fat:70},{calorieDelta:-100});assert.ok(Math.abs(t.protein*4+t.carbs*4+t.fat*9-t.kcal)<=4);});