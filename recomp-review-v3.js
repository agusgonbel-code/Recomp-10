(function(root,factory){'use strict';const api=factory();if(typeof module!=='undefined'&&module.exports)module.exports=api;if(root)root.RecompReview=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';const clamp=(v,a,b)=>Math.min(b,Math.max(a,Number(v)));const finite=(v,f=0)=>Number.isFinite(Number(v))?Number(v):f;const avg=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:0;
function parseDate(value){if(!value)return null;const d=new Date(String(value).length===10?String(value)+'T12:00:00':value);return Number.isNaN(d.getTime())?null:d.getTime();}
function linearSlope(xs,ys){const mx=avg(xs),my=avg(ys);let num=0,den=0;for(let i=0;i<xs.length;i++){const dx=xs[i]-mx;num+=dx*(ys[i]-my);den+=dx*dx;}return den?num/den:0;}
function seriesTrend(values=[],dates=[]){const raw=Array.isArray(values)?values:[],rows=[];for(let i=0;i<raw.length;i++){const value=Number(raw[i]);if(!Number.isFinite(value))continue;const ms=parseDate(Array.isArray(dates)?dates[i]:null);rows.push({value,ms});}if(rows.length<4)return{enough:false,delta:0,ratePct:0,spanDays:0,observations:rows.length};const haveDates=rows.every(x=>Number.isFinite(x.ms));if(haveDates)rows.sort((a,b)=>a.ms-b.ms);const x=haveDates?rows.map(r=>r.ms/864e5):rows.map((_,i)=>i*7),y=rows.map(r=>r.value),spanDays=Math.max(0,x.at(-1)-x[0]);if(spanDays<7)return{enough:false,delta:0,ratePct:0,spanDays:Number(spanDays.toFixed(1)),observations:rows.length};const n=Math.min(3,Math.floor(rows.length/2)),first=avg(y.slice(0,n)),last=avg(y.slice(-n)),weekly=linearSlope(x,y)*7;return{enough:true,first:Number(first.toFixed(2)),last:Number(last.toFixed(2)),delta:Number(weekly.toFixed(3)),ratePct:first?Number((weekly/first*100).toFixed(3)):0,spanDays:Number(spanDays.toFixed(1)),observations:rows.length,timing:haveDates?'dated':'weekly-assumed'};}
function review(input={}){const weight=seriesTrend(input.weights,input.weightDates),waist=seriesTrend(input.waist,input.waistDates),dietAdherence=clamp(finite(input.dietAdherence,.8),0,1),trainingAdherence=clamp(finite(input.trainingAdherence,.8),0,1),performance=clamp(finite(input.performance,0),-1,1),photoStandardized=Boolean(input.photoStandardized),weeks=Math.max(1,Math.round(finite(input.weeksObserved,1)));const reliable=dietAdherence>=.8&&trainingAdherence>=.7&&weight.enough;let status='insufficient-data',delta=0,confidence='low',signals=[];if(!weight.enough)signals.push('Faltan al menos 4 registros de peso distribuidos durante 7 días o más.');if(input.waist?.length&&!waist.enough)signals.push('Añade al menos 4 medidas de cintura separadas en el tiempo y tomadas en condiciones similares.');if(!reliable)signals.push('No se ajustan calorías hasta disponer de adherencia y datos suficientemente fiables.');if(reliable){confidence=weeks>=3&&waist.enough?'high':'medium';const stableWeight=Math.abs(weight.ratePct)<=.2,waistDown=waist.enough&&weeks>=4&&waist.delta<=-.1,waistFlat=!waist.enough||Math.abs(waist.delta)<.1;if(waistDown&&stableWeight&&performance>=-.2){status='positive-recomp';signals.push('Cintura descendente de forma sostenida con peso estable y rendimiento conservado: señal compatible con recomposición positiva.');}else if(weight.ratePct<-.6&&performance<-.25){status='too-aggressive';delta=100;signals.push('La pérdida semanal estimada es rápida y el rendimiento cae: se aumenta energía de forma conservadora.');}else if(weeks>=3&&waistFlat&&weight.ratePct>-.15&&dietAdherence>=.9){status='stalled';delta=-100;signals.push('Tres o más semanas sin cambio útil con alta adherencia: se reduce energía de forma conservadora.');}else{status='on-track';signals.push('No hay una señal suficientemente fuerte para modificar el plan: mantener y volver a medir.');}if(photoStandardized)signals.push('Las fotos estandarizadas sirven como contexto visual, no como medición aislada automática.');}
return{status,confidence,reliable,weight,waist,calorieDelta:delta,signals};}
function apply(targets={},reviewResult={}){const delta=finite(reviewResult.calorieDelta,0),kcal=Math.max(1200,Math.round(finite(targets.kcal,2000)+delta)),protein=Math.round(finite(targets.protein,0)),fat=Math.round(finite(targets.fat,0)),carbs=Math.max(0,Math.round((kcal-protein*4-fat*9)/4));return{kcal,protein,carbs,fat};}
return{seriesTrend,review,apply};});